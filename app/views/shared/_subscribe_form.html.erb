<button id="subscribe" class="intro-btn">Alright! Subscribe me</button>

  <div class="subscribe-form">
    <div class="row">
      <div class="col-md-10 col-md-offset-1">      <%= form_for(@subscription) do |f| %>
      <% if @subscription.errors.any? %>
        <div id="error_explanation">
          <h2><%= pluralize(@subscription.errors.count, "error") %> prohibited this user from being saved:</h2>

          <ul>
          <% @subscription.errors.full_messages.each do |message| %>
            <li><%= message %></li>
          <% end %>
          </ul>
        </div>
      <% end %>

        <div class="form-group">
          <%= f.text_field :email_address, placeholder: "Email Address", class: "form-control" %>
        </div>

        <div class="form-group">
          <%= f.text_field :name, placeholder: "Your Name", class: "form-control" %>
        </div>

        <p>Select your favorite shows:<p>

        <% @series.each do |show| %>
          <div class="checkbox-inline">
            <%= check_box_tag "series_ids[]", show.id %>
            <%= label_tag show.id, show.name %>
          </div>
        <% end %>

        <div class="actions">
          <%= f.submit "Subscribe", class: "btn btn-default sub-btn" %>
        </div>
    <%   end %>
      </div>
    </div>
  </div>
